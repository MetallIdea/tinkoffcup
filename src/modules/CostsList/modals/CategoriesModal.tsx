import { observer } from 'mobx-react-lite';
import { Dialog } from 'primereact/dialog';

export const CategoriesModal = observer(function CategoriesModal() {
  const handleHide = () => {

  }
  return (
    <Dialog onHide={handleHide}>
      Not implemented
    </Dialog>
  )
})
